spring:
  application:
    name: classe-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/classe_service?serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: passer123

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: true

server:
  port: 8087

springdoc:
  api-docs:
    path: /api-docs
